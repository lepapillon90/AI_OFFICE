/// Desktop-only build (web platform dropped) — no native file picker is
/// wired up yet, so this always resolves to null. See pick_file_stub.dart.
export 'pick_file_types.dart';
export 'pick_file_stub.dart';
